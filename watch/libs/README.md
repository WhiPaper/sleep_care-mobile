Drop the Samsung Health Sensor SDK AAR here when you have it.

The module is wired to include `*.aar` files from this folder, but the
current scaffold intentionally compiles and runs without the SDK present.
